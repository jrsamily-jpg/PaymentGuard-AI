# Optional: leave unset to use local data/workspaces/workspaces.db.
# WORKSPACE_DATABASE_URL=postgresql+psycopg://USER:PASSWORD@localhost:5432/paymentguard
# Docker Compose requires a strong, unique URL-safe database password:
# POSTGRES_PASSWORD=
# Accounts are created individually in the application; no shared API keys.
